import { LoaderComponent } from '@/components/Loaders/Loader.component'

export default function Loading() {
	// todo custom loading component with skeleton / base page
	return <LoaderComponent></LoaderComponent>
}
